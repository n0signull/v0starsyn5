# Star Syndicate Supabase

The migration defines the MVP schema, explicit Data API grants, RLS policies, profile creation trigger, seeded interests, and public image buckets with owner-scoped writes.

The browser receives only `NEXT_PUBLIC_SUPABASE_URL` and a publishable key. Never add a secret or service-role key to frontend environment variables.

